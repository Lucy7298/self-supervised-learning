# @package _global_

# to execute this experiment run:
# python main.py experiment=linear_fit.yaml

# to debug this experiment run:
# python main.py experiment=linear_fit.yaml trainer=debug_cpu.yaml

defaults:
  - override /trainer: default.yaml
  - override /model: byol_eval.yaml
  - override /train_dataset: imagenette_finetune.yaml
  - override /val_dataset: imagenette_val.yaml
  - override /logger: wandb_linear_fit.yaml
  - override /callbacks: linear_fit_callbacks.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

seed: 12345

model: 
  kwargs: 
    model_class: awesome_ssl.models.byol_lucidrains.SelfSupervisedLearner
    weight_path: /mnt/nfs/home/yunxingl/byol-pytorch/examples/lightning/byol-lucidrains/3dxbi8tn/checkpoints/epoch=149-step=11099.ckpt

logger: 
  wandb: 
    group: byol-lucidrains/3dxbi8tn

num_workers: 4