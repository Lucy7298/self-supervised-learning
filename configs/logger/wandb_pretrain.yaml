wandb:
  _target_: pytorch_lightning.loggers.wandb.WandbLogger
  project: "self-supervised-experiments"
  save_dir: "."
  offline: False # set True to store all logs only locally
  # entity: ""  # set to name of your wandb team
  log_model: False
  prefix: ""
  tags: []